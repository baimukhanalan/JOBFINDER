# Secrets - NEVER commit
.env
.env.local
.env.*.local
.env.production
*.pem
*.key
secrets/

# Real people's PII / sessions - NEVER commit (keep .example.json + .template.json only)
backend/data/profiles.json
backend/data/profiles.real.json
backend/data/proxies.json
backend/data/proxies.txt
backend/data/proxies_raw.txt
# validated US-egress pick cache (us_egress.py) — holds proxy creds, TTL'd
backend/data/us_egress_cache.json
# Runtime cache — rebuilt by `mass_hiring.collect()` / `everify_employers --refresh` (public data)
backend/data/everify_employers.json
backend/data/shl_answer_bank.json
backend/data/shl_assess_state.json
backend/data/shl_assess_done.json
backend/data/shl_assess_runner.lock
# assessment question-bank HARVESTER (runtime data + PII source + big regenerable media assets)
backend/data/assessment_bank.json
backend/data/assessment_media/
# qa_bot snapshot source (questions.jsonl + SHL_answers_all.csv) — SHL question content, big, gitignored;
# it feeds import_qa_snapshot.py into the (also gitignored) assessment_bank.json.
backend/data/qa_snapshot/
backend/data/harvest_state.json
# discovery.py's already-probed company-name cache (best-effort optimization, grows via cron)
backend/data/discovery_probed_names.json
backend/tools/assessment_harvester/assets/*.wav
backend/tools/assessment_harvester/assets/*.y4m
backend/data/*stealth_profile*/
backend/data/apply_campaigns.json
backend/data/apply_campaign_events.json
backend/data/mh_apply_cron.lock
backend/data/*.lock
# per-lane harvest_runner runtime locks (harvest_runner.lock.<suffix> — the .lock is
# NOT the extension, so backend/data/*.lock above does not catch them)
backend/data/harvest_runner.lock.*
backend/data/facts/*
!backend/data/facts/sample.json
backend/data/etalons/*
!backend/data/etalons/sample.json
data_sessions/
uploads/
data/profiles.json

# Dependencies
node_modules/
venv/
.venv/
env/
__pycache__/
*.pyc
*.pyo
.Python

# Build output
.next/
dist/
build/
out/
*.egg-info/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db
*.swp
*.swo

# IDE
.vscode/
.idea/
*.sublime-*

# Testing
coverage/
.pytest_cache/
.coverage
htmlcov/

# Misc
.cache/
tmp/
temp/
backend/.assist_token
uploads/answer_cache.db
# Extension: real identity + live token stay local (commit the .example.js versions)
extension/profile.js
extension/background.js
extension/apply-assist.zip

# --- Extra PII/secret exclusions (data backups, address map, generated outputs) ---
# Timestamped backups of profiles/facts/mail_addresses hold the same PII as the
# originals, so they must never be committed either.
backend/data/*.bak
backend/data/*.bak-*
backend/data/facts.bak-*/
backend/data/mail_addresses.json
# Generated dashboards / screenshots can embed candidate data.
outputs/
backend/data/mailbox_passwords.json

# local logs + crontab backups (never commit — may contain other projects secrets)
logs/
backend/data/demo_personas.json
backend/data/demo_used_names.json

# Residential-proxy tunnel key + filled installers (secret) — templates+generator ARE committed
backend/tools/residential_proxy/dist/
backend/tools/residential_proxy/client/key.pem
logs/icims_recon/
backend/data/icims_stealth_profile/

# Taleo candidate-account credentials (passwords) — PII, never commit
backend/data/taleo_accounts.json
backend/data/mh_settings.json
logs/crontab.bak-*

# never commit the secrets bundle (was public on GitHub 2026-08-19..09-05)
jobfinder-secrets.tar.gz
backend/data/mobile_proxy.json
# exit-node egress bridge: settings + per-slot state dirs (hold tailnet node keys) — never commit
backend/data/ts_egress.json
backend/data/ts-egress/
backend/.ts_authkey
backend/.ts_api_token
backend/.ts_authkey_public
# Runtime cache — resolved Zoom links for TP hiring events (rebuilt by hiring_events --refresh)
backend/data/hiring_events_zoom.json
data/workday_verified_tenants.json
data/workday_probe_blocked.json
# Auto-promoted iCIMS / Avature mass-hiring tenants (written by the self-verify probes) — data-driven
data/icims_verified_sources.json
# Auto-promoted Taleo-family mass-hiring sources (kaiser/percepta) — written by taleo_probe_promote
data/taleo_verified_sources.json
data/avature_verified_sources.json
# Auto-promoted Oracle-ORC mass-hiring sources (molina/hilton) — written by orc_probe_promote
data/orc_verified_sources.json
data/orc_probe_blocked.json
data/llm_breaker.json
data/roberthalf_verified.json
data/roberthalf_probe_blocked.json
data/adecco_verified.json
data/adecco_probe_blocked.json
data/progressive_verified.json
data/progressive_probe_blocked.json
data/randstad_verified.json
data/randstad_probe_blocked.json
data/afni_verified.json
data/afni_probe_blocked.json
